<project name="JunctionDocs" default="doclava" basedir=".">

<!-- To build documentation for TinySDK:

	1. Download Junction's source code:
		cd samples/junction; svn export http://prpl.stanford.edu/svn/prpl/junction/lib/JAVAJunction junction
	2. 
		rm doclava/src-list; find junction/src/main -name '*.java' >> doclava/src-list
		mvn assembly:assembly # builds a jar with dependencies

	2. run: `ant androiddoc`
		Documentation will be built for the files listed in doclava/src-list.
-->
  <property environment="env"/>
  <property name="javahome"
	value="${env.JAVA_HOME}"/>
  <property name="doclava.file" 
    value="../../build/dist/doclava-0.1.7/doclava-0.1.7.jar"/>

  <target name="doclava" description="Generate documentation">
    <taskdef name="doclava" classname="com.google.doclava.DoclavaTask" classpath="${doclava.file}"/>
      <doclava>
        <arguments>
        	-quiet
        	-bootclasspath "${javahome}/jre/lib/rt.jar${path.separator}${javahome}/jre/lib/jsse.jar"
        	-doclet com.google.doclava.Doclava
        	-docletpath ${doclava.file}
        	-classpath "junction/target/JAVAJunction-0.6.7-SNAPSHOT-jar-with-dependencies.jar"
        	-d build/api
 	       	-hdf project.name "Junction"
        	-stubs build/stubs
        	-apixml build/public_api.xml
        	@doclava/src-list
      	</arguments>
      </doclava>
  </target>

  <target name="clean" description="Remove generated files.">
    <delete dir="build"/> 
  </target>

</project>
